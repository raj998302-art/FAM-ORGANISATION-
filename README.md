# FIRE ARENA MAX
The Ultimate Free Fire MAX Tournament Platform

## Documentation
- Frontend is running on React/Vite.
- Backend is running on Express with MongoDB.
- To run locally, run `npm install` and then `npm run dev`.
